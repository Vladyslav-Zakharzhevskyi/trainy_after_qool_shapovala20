version: "3.8"
services:
  web:
    image: arkansas3000/invest-front-image:latest
    container_name: web-service
    restart: "no"
    ports:
      - 5000:80
    depends_on:
      - backend
  backend:
    image: arkansas3000/invest-back-image:initial
    container_name: backend-service
    ports:
      - 8080:5000
    restart: "no"
    depends_on:
      - postgresdb
  postgresdb:
    image: postgres
    container_name: postgresdb-service
    restart: "no"
    ports:
      - 5432:5432
    volumes:
      - /usr/vlad/docker-compose/mount/postgresql:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=pspostgres
      - POSTGRES_DB=app

